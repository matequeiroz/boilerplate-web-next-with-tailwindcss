import { Meta, StoryObj } from '@storybook/react'
import { Avatar } from '.'

export default {
  title: 'components/Avatar',
  component: Avatar,
  parameters: {
    layout: 'fullscreen'
  }
} as Meta

export const Default: StoryObj = {}
